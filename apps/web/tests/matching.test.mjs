import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("customer Smart Match explains deterministic fallback and safe signals",async()=>{const source=await readFile(new URL("../app/dashboard/projects/[id]/recommendations/page.tsx",import.meta.url),"utf8");assert.match(source,/Детерминированный подбор доступен без AI/);assert.match(source,/навыки, категорию, похожие работы/);assert.match(source,/reasons\.slice\(0,5\)/);assert.match(source,/не гарантия результата/);assert.doesNotMatch(source,/AI says|perfect match/i)});
test("manual concierge is explicitly platform-labelled and admin scoped",async()=>{const source=await readFile(new URL("../app/x7m4q9k2/matching/page.tsx",import.meta.url),"utf8");assert.match(source,/Ручной подбор/);assert.match(source,/Рекомендация сохранена и отмечена как платформенная/);assert.match(source,/admin\/projects/);assert.match(source,/admin\/metrics\/ai/)});
