import Breadcrumbs from "../../breadcrumbs";
import AcceptInvite from "./accept-invite";
type Preview={type:string;project_id?:string;project_title?:string;category_name?:string;inviter_display_name:string;invited_role:string;expires_at:string;accepted:boolean};
async function load(token:string):Promise<Preview|null>{const base=process.env.API_BASE_URL??"http://localhost:8080";const response=await fetch(`${base}/api/v1/invites/${encodeURIComponent(token)}/preview`,{cache:"no-store"});if(!response.ok)return null;return(await response.json()).data??null}
export default async function InvitePage({params}:{params:Promise<{token:string}>}){const {token}=await params;const invite=await load(token);if(!invite)return <main><h1>Приглашение недоступно</h1><p>Ссылка истекла, уже недействительна или была введена неверно.</p></main>;return <main><Breadcrumbs items={[{label:"Главная",href:"/"},{label:"Приглашение"}]}/><h1>Приглашение к сотрудничеству</h1><p><strong>{invite.inviter_display_name}</strong> приглашает вас как {invite.invited_role==="CUSTOMER"?"заказчика":"исполнителя"}.</p>{invite.project_title?<section><h2>{invite.project_title}</h2>{invite.category_name?<p>{invite.category_name}</p>:null}<p>Другие отклики, вложения и приватные данные здесь не показываются.</p></section>:null}<p>Ссылка действует до <time dateTime={invite.expires_at}>{new Date(invite.expires_at).toLocaleDateString("ru-RU")}</time>.</p><AcceptInvite token={token} accepted={invite.accepted}/></main>}
