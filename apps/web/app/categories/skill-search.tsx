"use client";import { useEffect,useState } from "react";type Skill={id:string;name:string};export default function SkillSearch(){const[q,setQ]=useState("");const[items,setItems]=useState<Skill[]>([]);useEffect(()=>{if(!q){setItems([]);return}const controller=new AbortController();const timer=setTimeout(()=>fetch(`/api/v1/skills?q=${encodeURIComponent(q)}`,{signal:controller.signal}).then(r=>r.ok?r.json():Promise.reject()).then(b=>setItems(b.data??[])).catch(()=>{}),200);return()=>{clearTimeout(timer);controller.abort()}},[q]);return <section><h2>Найти навык</h2><label>Навык <input type="search" maxLength={100} value={q} onChange={e=>setQ(e.target.value)}/></label>{items.length?<ul>{items.map(v=><li key={v.id}>{v.name}</li>)}</ul>:null}</section>}
