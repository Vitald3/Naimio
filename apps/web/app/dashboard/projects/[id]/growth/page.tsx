"use client";
import Breadcrumbs from "../../../../breadcrumbs";
import{use,useState}from"react";
export default function ProjectGrowthPage({params}:{params:Promise<{id:string}>}){const { id } = use(params);const[state,setState]=useState("");const[link,setLink]=useState("");async function share(){const r=await fetch(`/api/v1/me/projects/${id}/share`,{method:"POST",credentials:"same-origin"});const b=await r.json();if(r.ok){setLink(b.data.url);setState("Ссылка готова")}else setState("Можно делиться только своим открытым публичным проектом")}async function repeat(){const invite=confirm("Пригласить предыдущего исполнителя в новый проект?");const r=await fetch(`/api/v1/me/projects/${id}/repeat`,{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/json"},body:JSON.stringify({invite_previous_freelancer:invite})});const b=await r.json();if(r.ok){setState("Черновик создан — проверьте его перед публикацией");location.assign(`/dashboard/projects/${b.data.project_id}`)}else setState("Повторить можно только свой завершённый проект")}return <main><Breadcrumbs items={[{label:"Главная",href:"/"},{label:"Кабинет",href:"/dashboard"},{label:"Проект",href:`/dashboard/projects/${id}`},{label:"Повторная работа"}]}/><h1>Повторная работа</h1><p>Новый проект создаётся чистым черновиком: без прежних откликов, отзывов, переписки и назначения.</p><button type="button" onClick={share}>Получить публичную ссылку</button><button type="button" onClick={repeat}>Повторить проект</button>{link?<p><a href={link}>{link}</a> <button type="button" onClick={()=>navigator.clipboard.writeText(link)}>Копировать</button></p>:null}<p role="status">{state}</p></main>}
