"use client";
import { CustomSelect } from "../../../../custom-select";
import Breadcrumbs from "../../../../breadcrumbs";
import { use, FormEvent, useEffect, useState } from "react";
type Vacancy={title:string;slug:string;description:string;employment_type:string;salary_min_kopecks?:number;salary_max_kopecks?:number;currency:string;location?:string;remote:boolean;experience_level?:string};
export default function EditVacancy({params}:{params:Promise<{id:string}>}){const { id } = use(params);const[v,setV]=useState<Vacancy|null>(null);const[state,setState]=useState("");useEffect(()=>{fetch(`/api/v1/me/vacancies/${id}`).then(r=>r.ok?r.json():Promise.reject()).then(b=>setV(b.data)).catch(()=>setState("Вакансия не найдена или недоступна."));},[id]);async function submit(e:FormEvent){e.preventDefault();if(!v)return;const r=await fetch(`/api/v1/me/vacancies/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(v)});setState(r.ok?"Изменения сохранены.":"Редактировать можно только свой черновик.")};if(!v)return <main><p>{state||"Загружаем…"}</p></main>;return <main><Breadcrumbs items={[{label:"Главная",href:"/"},{label:"Кабинет",href:"/dashboard"},{label:"Мои вакансии",href:"/dashboard/vacancies"},{label:"Редактировать"}]}/><h1>Редактировать вакансию</h1><form onSubmit={submit}><label>Название <input required maxLength={200} value={v.title} onChange={e=>setV({...v,title:e.target.value})}/></label><br/><label>Slug <input required maxLength={240} pattern="[a-z0-9]+(?:-[a-z0-9]+)*" data-pattern-message="Используйте латинские буквы, цифры и дефисы, например senior-go-developer" value={v.slug} onChange={e=>setV({...v,slug:e.target.value})}/></label><br/><label>Описание<br/><textarea required maxLength={20000} rows={8} value={v.description} onChange={e=>setV({...v,description:e.target.value})}/></label><br/><label>Занятость <CustomSelect value={v.employment_type} onChange={e=>setV({...v,employment_type:e.target.value})}><option value="FULL_TIME">Полная</option><option value="PART_TIME">Частичная</option><option value="CONTRACT">Контракт</option><option value="INTERNSHIP">Стажировка</option></CustomSelect></label><br/><label>Локация <input maxLength={160} value={v.location??""} onChange={e=>setV({...v,location:e.target.value})}/></label><br/><label><input type="checkbox" checked={v.remote} onChange={e=>setV({...v,remote:e.target.checked})}/> Удалённо</label><br/><button>Сохранить</button></form>{state?<p role="status">{state}</p>:null}</main>}
