"use client";
import Breadcrumbs from "../../../breadcrumbs";
import{use,useEffect,useState}from"react";import ProposalForm from"../../../project/[id]/proposal-form";
type Project={id:string;title:string;description:string;category_name?:string;visibility:string;status:string;customer_display_name:string};
export default function InvitedProjectPage({params}:{params:Promise<{id:string}>}){const { id } = use(params);const[item,setItem]=useState<Project|null>(null);const[missing,setMissing]=useState(false);useEffect(()=>{fetch(`/api/v1/me/invited-projects/${id}`,{credentials:"same-origin"}).then(r=>r.ok?r.json():Promise.reject()).then(b=>setItem(b.data)).catch(()=>setMissing(true))},[id]);if(missing)return <main><h1>Проект недоступен</h1><p>Приглашение не принадлежит вашему аккаунту или проект больше не принимает отклики.</p></main>;if(!item)return <main><p>Загружаем приглашённый проект…</p></main>;return <main><Breadcrumbs items={[{label:"Главная",href:"/"},{label:"Кабинет",href:"/dashboard"},{label:"Приглашения",href:"/dashboard/invites"},{label:item.title}]}/><p>{item.category_name}</p><h1>{item.title}</h1><p>Заказчик: {item.customer_display_name}</p><p>{item.description}</p><p>Этот приватный проект доступен только благодаря принятому приглашению.</p><ProposalForm projectId={item.id}/></main>}
